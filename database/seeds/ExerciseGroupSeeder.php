<?php

use App\Models\Exercise;
use App\Models\Group;
use Illuminate\Database\Seeder;

class ExerciseGroupSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
//        $exercises = Exercise::get();

//        $exercises->each(function ($exercise) {
//            $groups = $exercise->coach->groups;
//            $exercise->groups()->save($groups->random(), ['break_duration' => '01:00', 'order' => rand(0, 20)]);
//        });
    }
}
