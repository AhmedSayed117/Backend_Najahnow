<?php

use App\Models\Group;
use App\Models\Set;
use Illuminate\Database\Seeder;

class SetGroupSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
//        $sets = Set::get();
//
//
//        $sets->each(function ($set) {
//            $groups = $set->coach->groups;
//            $set->groups()->save($groups->random(), ['break_duration' => '1 hour', 'order' => rand(0, 20)]);
//        });
    }
}
